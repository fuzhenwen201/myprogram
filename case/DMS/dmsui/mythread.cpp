#include "mythread.h"
/*线程处理函数*/
void mythread::run()
{
	emit sigUi(list<Matchedlog>* match);
	sleep(1);
	emit sigUi(list<Matchedlog>* match);
	sleep(1);
	emit sigUi(list<Matchedlog>* match);
	sleep(1);
	emit sigUi(list<Matchedlog>* match);
	sleep(1);
}
